//MULTIDIMENSIONAL ARRAY:
/*
    A multi-dimensional array is an array that contains other arrays as its elements. Multi-dimesnional arrays can be defined using the same notation as one-dimensional arrays, but with nested square brackets.
*/
var matrix = [
    [1, 2],
    [3, 4],
];
console.log(matrix);
