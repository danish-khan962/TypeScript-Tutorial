//ANNOTATIONS

/* 
    Annotations are used to specify the data type of a variable, parameter, function , return value, and other types of values.
    Annotations help developers catch errors early in the development by allowing them to specify what type of values can be assigned to a given variable or passed as an argument to a function.
*/

//String annotation:
var name: string = "Danish";
name = "Shifa";
console.log(name);


//Number annotation:
let num: number = 50;
num = 100;
console.log(num);
